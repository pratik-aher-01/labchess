# ─────────────────────────────────────────────
#  LabChess — .gitignore
# ─────────────────────────────────────────────

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor & IDE folders
.vscode/
.idea/
*.suo
*.user

# Firebase
.firebase/
firebase-debug.log*
firestore-debug.log*
ui-debug.log*

# Node / Dependencies
node_modules/
package-lock.json

# Service Account Keys & Secrets
*serviceAccountKey*.json
.env
.env.local
.env.*.local

# Firebase config with live credentials — inlined in index.html instead
# See config.example.js for the template
config.js

# Local dev server tooling (not needed in production)
package.json